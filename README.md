# toto app

## Installation 

### Install pyenv

    $ brew update
    $ brew install pyenv
    $ echo 'eval "$(pyenv init --path)"' >> ~/.zprofile

### Prepare environment

    $ pyenv install

After successful installation 
    
    $ pyenv local

### Install dependencies